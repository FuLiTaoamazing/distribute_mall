spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://10.15.0.5:3307/mall_ums?characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: MALL-MEMBER
  cloud:
    nacos:
      server-addr: 10.15.0.3:8848
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  global-config:
    db-config:
      id-type: auto
logging:
  level:
    com.flt.member.dao: debug
server:
  port: 8000
